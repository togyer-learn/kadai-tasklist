<% if logged_in? %>
  <p>ログイン完了ユーザー： <%= current_user.name %></p>

  <h1>タスク一覧</h1>
  
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.id, task %> : <%= task.content %> ｜ <%= task.status %></li>
    <% end %>
  </ul>
  <%= paginate @tasks %>
  
  <%= link_to 'タスクの追加', new_task_path %>
  <%= link_to 'Logout', logout_path, method: :delete %>
<% else %>
  <%= redirect_to login_path %>
<% end %>